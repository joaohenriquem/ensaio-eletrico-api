name: Reativar API (08:30 BRT)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'    # 04:00 BRT — disparo antecipado (atraso GitHub ~3-5h → acorda até 09:00 BRT)
    - cron: '30 11 * * *'  # 08:30 BRT — backup caso o primeiro atrase demais

jobs:
  reativar:
    runs-on: ubuntu-latest
    steps:
      - name: Reativar API no Render
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/resume" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json")
          echo "Resposta: $RESPONSE"
